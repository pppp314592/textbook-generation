# ガロア理論

- 対象読者: 大学3〜4年生、大学院入試対策
- レベル: 上級（大学3〜4年・院入試）
- 前提知識: 線形代数、群・環・体の初歩（第1章で復習）
- 想定学習時間: 40〜60時間

## 目次

1. [はじめに](#はじめに)
2. [第1章 群・環・体の基礎](#第1章-群環体の基礎)
3. [第2章 体の拡大と拡大次数](#第2章-体の拡大と拡大次数)
4. [第3章 代数拡大と最小多項式](#第3章-代数拡大と最小多項式)
5. [第4章 分解体と代数閉包](#第4章-分解体と代数閉包)
6. [第5章 分離拡大と有限体](#第5章-分離拡大と有限体)
7. [第6章 正規拡大](#第6章-正規拡大)
8. [第7章 ガロア拡大とガロア群](#第7章-ガロア拡大とガロア群)
9. [第8章 ガロア理論の基本定理](#第8章-ガロア理論の基本定理)
10. [第9章 応用：作図問題と方程式の可解性](#第9章-応用作図問題と方程式の可解性)
11. [第10章 総まとめと演習](#第10章-総まとめと演習)
12. [付録A 練習問題の解答](#付録a-練習問題の解答)
13. [付録B 用語集](#付録b-用語集)
14. [付録C 記号表](#付録c-記号表)

---

## はじめに

ガロア理論は、19世紀初頭にエヴァリスト・ガロア（Évariste Galois, 1811–1832）によって創始された、**体（たい）と群（ぐん）の対応**を扱う理論です。その中心となる発見は、次のようなものです。

> **多項式方程式が「四則演算と冪根」だけで解けるかどうかは、その方程式に付随する対称性（ガロア群）の構造で完全に決まる。**

この教科書の最終ゴールは、次の命題を厳密に証明することです。

> **一般の5次以上の代数方程式は、四則演算と冪根だけでは解けない（アーベル–ルフィニの定理）。**

ガロア理論は代数学の頂点の一つであり、可換環論・代数幾何・整数論（類体論）などの土台にもなります。この本では、前提となる群・環・体の基礎を第1章で復習し、第2章以降で体拡大の理論を段階的に積み上げ、第8章で「ガロア理論の基本定理」に到達し、第9章で5次方程式の問題を解決します。

### この本の使い方

- 各章は「**学習目標** → 本文 → **本章のまとめ** → **練習問題**」の構成です。
- 練習問題の解答は付録Aに解説付きであります。まず自分の力で解いてから見てください。
- 院試対策として、各章の練習問題に加えて第10章に総合演習を用意しました。
- 証明はこの本の流れで必要なものに絞りましたが、院試では証明も問われるため、**太字の命題・定理は証明まで含めて理解する**ことをおすすめします。

### 前提知識と記号

- 線形代数（基底、次元、線型写像）を前提とします。
- 群・環・体の定義は第1章で復習しますが、一度学んだことがあると理解が速いです。
- 記号は付録Cにまとめてあります（例：`[L:K]` 拡大次数、`Gal(L/K)` ガロア群）。

それでは、第1章から始めましょう。

---

## 第1章 群・環・体の基礎

### 学習目標

- 群・部分群・正規部分群・準同型の定義と基本性質を確認できる。
- 剰余群と準同型定理（第1同型定理）を使える。
- 環・イデアル・整域・体の定義を確認できる。
- 体の標数と素体を理解できる。

### 1.1 群の定義と例

**定義（群）** 集合 G に二項演算 `·: G×G → G` が定義されていて、次の3条件を満たすとき、(G, ·) を**群**と呼ぶ。

1. **結合律**: 任意の a, b, c ∈ G に対して (a·b)·c = a·(b·c)。
2. **単位元の存在**: 任意の a ∈ G に対して a·e = e·a = a となる元 e ∈ G が存在する。
3. **逆元の存在**: 任意の a ∈ G に対して a·b = b·a = e となる b ∈ G が存在する。

演算が可換（a·b = b·a が常に成り立つ）である群を**可換群**（または**アーベル群**）と呼ぶ。以後、演算は文脈により `+`（加法群）や `·`（乗法群）で表す。

**例 1.1**
- 整数全体 Z は加法に関して可換群（単位元 0、逆元 −a）。
- 有理数全体 Q、実数全体 R、複素数全体 C は加法に関して可換群。
- 0 を除いた有理数全体 Q* は乗法に関して可換群。
- n次正則行列全体 GL_n(R) は行列の積に関して群（一般線型群）。n ≥ 2 なら非可換。

**定義（部分群）** 群 G の部分集合 H が、G の演算に関してそれ自身群になるとき、H を G の**部分群**と呼び、`H ≤ G` と書く。H が部分群であることは「H が空でなく、a, b ∈ H ⇒ a·b⁻¹ ∈ H」と同値である。

### 1.2 正規部分群と剰余群

**定義（正規部分群）** 部分群 H ≤ G が、任意の g ∈ G, h ∈ H に対して `g·h·g⁻¹ ∈ H` を満たすとき、H を**正規部分群**と呼び、`H ⊴ G` と書く。特に可換群のすべての部分群は正規部分群である。

**定義（剰余類と剰余群）** H ≤ G、g ∈ G に対し、`gH = { g·h | h ∈ H }` を H の（左）剰余類と呼ぶ。G を左剰余類に分解した集合を `G/H` と書く。H が正規部分群のとき、G/H は `(g₁H)·(g₂H) = (g₁·g₂)H` によって**群**になる。これを**剰余群**と呼ぶ。剰余類の個数 `|G/H|` を**指数**といい、`[G:H]` と書く。

**定理（ラグランジュの定理）** 有限群 G とその部分群 H に対して `|G| = |H| · [G:H]`。特に `|H|` は `|G|` を割り切る。

### 1.3 準同型と準同型定理

**定義（群準同型）** 群 G₁, G₂ の間の写像 `f: G₁ → G₂` が、任意の a, b ∈ G₁ に対して `f(a·b) = f(a)·f(b)` を満たすとき、f を**群準同型**と呼ぶ。全単射な準同型を**同型**と呼び、同型 f が存在するとき G₁ ≅ G₂ と書く。

- **核**: `Ker f = { a ∈ G₁ | f(a) = e₂ }` は G₁ の正規部分群。
- **像**: `Im f = { f(a) | a ∈ G₁ }` は G₂ の部分群。

**定理（準同型定理／第1同型定理）** 準同型 f: G₁ → G₂ に対して

```
G₁ / Ker f  ≅  Im f
```

が成り立つ。

**例 1.2** `f: Z → Z/nZ`（剰余を取る写像）は全射準同型。Ker f = nZ だから `Z/nZ ≅ Z/nZ`（自明だが、準同型定理の枠組みそのもの）。

### 1.4 環とイデアル

**定義（環）** 集合 R に2つの演算（加法 `+` と乗法 `·`）が定義され、次の条件を満たすとき (R, +, ·) を**環**と呼ぶ。

1. (R, +) は可換群（単位元 0）。
2. 乗法は結合律を満たし、加法に対して分配律 `a·(b+c) = a·b + a·c` が成り立つ。

さらに乗法が可換で、乗法単位元 1（≠ 0）を持つ環を**可換環**と呼ぶ。この本では特に断らない限り**可換環**を考える。

**定義（イデアル）** 可換環 R の部分集合 I が次の条件を満たすとき、**イデアル**と呼ぶ。

1. (I, +) は (R, +) の部分群。
2. 任意の r ∈ R, a ∈ I に対して `r·a ∈ I`。

**例 1.3**
- Z のイデアルは nZ = { n·k | k ∈ Z } のみ。
- 体 K 上の多項式環 K[X] のイデアルは、ある多項式 f で生成された `(f) = { f·g | g ∈ K[X] }` のみ。

イデアル I による剰余環 `R/I` は `(a+I)·(b+I) = (a·b)+I` で環になる。

**定義（整域・極大イデアル）**
- **整域**: a·b = 0 なら a = 0 または b = 0 を満たす可換環（零因子を持たない可換環）。
- **極大イデアル**: 真のイデアルで、それより大きい真のイデアルが存在しないもの。**I が R の極大イデアル ⇔ R/I が体** が成り立つ（重要）。

### 1.5 体の定義と例

**定義（体）** 可換環 K であって、0 以外のすべての元が乗法逆元を持つものを**体**と呼ぶ。

**例 1.4**
- Q, R, C は体（標数 0）。
- p を素数とするとき、剰余環 Z/pZ は体。これを `F_p` と書く（有限体の最も基本的な例）。
- 複素有理関数体 C(X)、有理数体上の多項式環の商体 Q(X) も体。

### 1.6 標数と素体

**定義（標数）** 体 K において、`1 + 1 + ⋯ + 1`（n個）が 0 になる最小の正の整数 n を**標数**と呼び、`char K` と書く。そのような n が存在しなければ標数 0 という。

- 標数 0 の体は Q を含む（Q を素体として含む）。
- 標数 p > 0 の体は F_p を素体として含む（p は素数）。
- 標数 p の体では二項定理の係数 pCi (1 ≤ i ≤ p−1) が p で割れることから、**フロベニウス写像** `φ: x ↦ x^p` が準同型になる（第5章で再登場）。

**命題（標数の基本性質）** 体の標数は 0 または素数である。

**例 1.5** `F_2 = Z/2Z` は標数 2 の体。`F_2` 上では `x^2 + 1 = (x + 1)^2` であり、標数の特殊性が現れる。

### 本章のまとめ

- 群は演算・単位元・逆元を持つ集合。正規部分群によって剰余群ができる。
- 準同型定理 `G₁/Ker f ≅ Im f` は群論の最重要定理の一つ。
- 可換環の極大イデアル I について R/I は体になる。
- 体の標数は 0 または素数。

### 練習問題

**練習問題 1.1** 群 G と部分群 H に対し、ラグランジュの定理を述べよ。また、位数 6 の群の部分群の位数としてあり得る値をすべて挙げよ。

**練習問題 1.2** `f: Z → Z/4Z` を剰余を取る準同型とする。Ker f を求め、準同型定理によって得られる同型を書け。

**練習問題 1.3** 可換環 R のイデアル I について「R/I が体であるための必要十分条件は I が R の極大イデアルであること」を述べた。これを認めて、Z のイデアル nZ のうち、Z/nZ が体になるものをすべて答えよ。

**練習問題 1.4** 体 K の標数が p（素数）であるとき、`φ: K → K`，`x ↦ x^p` は体準同型であることを示せ。

**練習問題 1.5** 標数 2 の体では `a + a = 0` が任意の元 a で成り立つことを示し、`(a + b)^2 = a^2 + b^2` となることを確かめよ。

---

## 第2章 体の拡大と拡大次数

### 学習目標

- 体拡大の定義を理解し、拡大次数 [L:K] の意味をつかむ。
- 拡大次数の連鎖律を使える。
- 代数拡大と超越拡大の区別ができる。

### 2.1 体の拡大

**定義（体拡大）** 体 K が体 L の部分集合で、K の演算が L の演算の制限になっているとき、`L/K` は**体拡大**であるといい、「L は K の拡大体」と呼ぶ。

**例 2.1**
- `R/Q`，`C/R`，`C/Q` は体拡大。
- F_p の有限拡大は有限体（第5章で詳述）。

### 2.2 拡大次数

体 L を体 K 上の**線型空間**とみなす。K 上の加法・スカラー倍が定義できているからである。

**定義（拡大次数）** K 上の線型空間としての L の次元を**拡大次数**と呼び、`[L:K]` と書く。`[L:K] < ∞` のとき有限次拡大、そうでないとき無限次拡大という。

**例 2.2**
- `[C:R] = 2`。実際、C = R + Ri で基底 {1, i}。
- `[R:Q]` は無限（Q を部分空間に持つ高々可算生成な構造ではない）。

### 2.3 拡大次数の連鎖律

**定理（拡大次数の連鎖律）** 体の拡大の列 K ⊂ L ⊂ M について

```
[M:K] = [M:L] · [L:K]
```

が成り立つ（ただし無限次元の場合は通常の意味での積として、一方が無限なら他方も無限）。

**証明（有限次の場合）** L の K 上の基底を {v₁, …, v_m}、M の L 上の基底を {w₁, …, w_n} とする。このとき {v_i w_j} (i=1..m, j=1..n) が M の K 上の基底になることを示せばよい。任意の x ∈ M は x = Σ_j a_j w_j（a_j ∈ L）と書け、各 a_j は a_j = Σ_i b_ij v_i（b_ij ∈ K）と書けるから、x = Σ_{i,j} b_ij (v_i w_j) と書け、生成する。一次独立性も同様に示される。□

**例 2.3** `Q ⊂ Q(√2) ⊂ Q(√2, √3)` とすると `[Q(√2):Q] = 2`，`[Q(√2,√3):Q(√2)] = 2`，よって `[Q(√2,√3):Q] = 4`。

### 2.4 代数拡大と超越拡大

**定義（代数的元・超越的元）** 体拡大 L/K と a ∈ L に対し、`f(a) = 0` となる 0 でない多項式 f ∈ K[X] が存在するとき、a は K 上**代数的**であるという。代数的でない元を**超越的**という。

**定義（代数拡大）** L のすべての元が K 上代数的であるとき、`L/K` は**代数拡大**であるという。

**例 2.4**
- √2, i, `∛2` は Q 上代数的（それぞれ X²−2, X²+1, X³−2 の根）。
- π, e は Q 上超越的（リンデマン、エルミートの定理）。
- `R/Q` は代数拡大ではない（超越元を含む）。

**命題** 有限次拡大は代数拡大である。

**証明** `[L:K] = n` とする。任意の a ∈ L に対し、`1, a, a², …, aⁿ` は n+1 個の元だから K 上一次従属。よって `c₀ + c₁a + ⋯ + cₙaⁿ = 0`（すべては 0 でない係数）が存在し、a は代数的。□

### 2.5 有限次拡大の例

**例 2.5** `L = Q(∛2)` を考える。`[L:Q] = 3` で基底は `{1, ∛2, (∛2)²}`。実際、X³−2 は Q 上既約（アイゼンシュタインの判定法：素数 2 で条件を満たす）で、最小多項式が X³−2。

**例 2.6** 単拡大 `K(a)` は、K に a を添加して得られる K 上の最小の体で、K 上の多項式環 K[X] の元を a で評価して得られる像全体 `{ f(a) | f ∈ K[X] }` の商体として構成される。次章で詳しく扱う。

### 本章のまとめ

- `L/K` を K 上の線型空間とみなし、その次元が拡大次数 [L:K]。
- 連鎖律 `[M:K] = [M:L][L:K]` は次数計算の基本。
- 有限次拡大は自動的に代数拡大。
- 代数的か超越的かは多項式の根になるかどうかで判定する。

### 練習問題

**練習問題 2.1** `[Q(√2):Q]` を求めよ。また `{1, √2}` が基底になることを示せ。

**練習問題 2.2** 連鎖律を用いて、`[Q(√2, √3, √5):Q]` を求めよ。

**練習問題 2.3** 標数 0 の体 K 上で、`[K(a):K]` が有限ならば a は K 上代数的であることを示せ。

**練習問題 2.4** 体拡大 `L/K` が代数拡大であるとき、`a, b ∈ L` の和 a+b、積 ab も K 上代数的であることを示せ。（ヒント: K(a) と K(b) を考える）

**練習問題 2.5** `Q(√2)` は Q の代数拡大であるが、`R/Q` は代数拡大でないことを例を挙げて示せ。

---

## 第3章 代数拡大と最小多項式

### 学習目標

- 最小多項式の定義と性質を理解する。
- 単拡大 K(a) の構造（K[X]/(f(X)) との同型）を理解する。
- 単拡大の拡大次数を計算できる。

### 3.1 代数的元と最小多項式

**定義（最小多項式）** L/K で a ∈ L が K 上代数的であるとする。`f(a) = 0` を満たす K[X] の多項式のうち、**モニック**（最高次の係数が 1）かつ次数最小のものを a の K 上の**最小多項式**と呼び、`irr(a, K)` と書く。

**命題（最小多項式の性質）** a の最小多項式 f(X) は K[X] において**既約**であり、`g(a) = 0` となる多項式 g(X) 全体はイデアル `(f(X))` に一致する。

**証明** `f = h·k` と因数分解されたとすると、f(a) = h(a)k(a) = 0 より h(a) = 0 または k(a) = 0。次数の最小性から一方は定数。よって既約。また、g(a) = 0 なる g に対して g を f で割った余り r（deg r < deg f）を考えると r(a) = 0 だから最小性より r = 0。したがって g ∈ (f)。□

### 3.2 単拡大 K(a)

**定義（単拡大）** 体拡大 L/K と a ∈ L に対して、K と a を含む L の最小部分体を `K(a)` と書き、K に a を添加した体という。`L = K(a)` のとき L/K を**単拡大**と呼ぶ。

**定理（単拡大の構造）** a を K 上代数的とし、f(X) = irr(a, K)（deg f = n）とする。このとき

1. 写像 `ψ: K[X]/(f(X)) → K(a)`，`g(X) + (f(X)) ↦ g(a)` は同型。
2. `[K(a):K] = n` で、`{1, a, a², …, a^(n−1)}` は K 上の基底。

**証明** `ψ` が準同型であることは明らか。f が既約で K[X] は単項イデアル整域なので `(f(X))` は極大イデアル、よって K[X]/(f(X)) は体。ψ は体の準同型だから単射。一方、K(a) は K と a で生成される体で、ψ の像 `{g(a)}` は K と a を含み K[X]/(f) は体だから像自身が K(a) に一致する（像は体である）。よって全射。次数については `{1, a, …, a^(n−1)}` が生成し、一次独立性は最小多項式の最小次数性から従う。□

**例 3.1** `a = √2`，K = Q とすると irr(√2, Q) = X²−2。よって `Q(√2) ≅ Q[X]/(X²−2)`，`[Q(√2):Q] = 2`。

**例 3.2** `a = ∛2` では irr(∛2, Q) = X³−2（既約：アイゼンシュタイン）。`[Q(∛2):Q] = 3`，基底 {1, ∛2, (∛2)²}。

### 3.3 単拡大の同型と共役

**命題（同型の延長）** a, b を K 上代数的な元とし、`irr(a, K) = irr(b, K)` とすると、`K(a) ≅ K(b)`（a を b に写す K 上の同型が存在する）。

これは上の同型 `K(a) ≅ K[X]/(f)`，`K(b) ≅ K[X]/(f)` を合成すればよい。同じ最小多項式の根のことを**共役**と呼ぶ。

### 3.4 拡大次数の計算技法

**連鎖律の利用例** `K ⊂ K(a) ⊂ K(a,b)` とみて `[K(a,b):K] = [K(a,b):K(a)]·[K(a):K]` を順に計算する。

**例 3.3** `L = Q(√2, √3)` を考える。`[Q(√2):Q] = 2`。`√3 ∉ Q(√2)` であること（仮に √3 = p + q√2 とすると両辺 2 乗して矛盾）から `[Q(√2,√3):Q(√2)] = 2`。よって `[L:Q] = 4`。

**例 3.4** `Q(∛2, ω)`（ω は 1 の原始3乗根）では `[Q(∛2, ω):Q] = 6`。実際 X³−2 の分解体で、ガロア群は S₃（第7章で詳述）。

### 3.5 代数拡大の推移性

**命題** 体の列 K ⊂ L ⊂ M で、M/L が代数拡大かつ L/K が代数拡大ならば、M/K も代数拡大である。

**証明** a ∈ M とする。a は L 上代数的だから、ある多項式 `g(X) = c₀ + c₁X + ⋯ + cₙXⁿ`（c_i ∈ L）が存在して g(a) = 0。各 c_i は K 上代数的。`L' = K(c₀, …, cₙ)` は有限次拡大（各添加は代数的なので有限次、連鎖律より有限次）。a は L' 上代数的なので `[L'(a):L'] < ∞`。連鎖律より `[L'(a):K] = [L'(a):L']·[L':K] < ∞`。よって a は K 上代数的（有限次拡大は代数拡大）。□

### 本章のまとめ

- 最小多項式は既約で、それで割り切れるかどうかで「根になること」が判定できる。
- 単拡大 `K(a) ≅ K[X]/(irr(a,K))` が基本。拡大次数は最小多項式の次数に等しい。
- 同じ最小多項式を持つ元は同型（共役）。
- 代数拡大の列は推移的。

### 練習問題

**練習問題 3.1** `a = √5` の Q 上の最小多項式を求め、`[Q(√5):Q]` を答えよ。

**練習問題 3.2** `a = √2 + √3` が Q 上代数的であることを示し、その最小多項式を求めよ。

**練習問題 3.3** `K = F_2`，`f(X) = X² + X + 1 ∈ K[X]` が既約であることを示し、`K[X]/(f)` が4元体であることを確かめよ。

**練習問題 3.4** `Q(∛2)` の元 `(∛2)² + ∛2` の逆元を求めよ。（ヒント: 最小多項式 X³−2 を用いて剰余を計算する）

**練習問題 3.5** 体拡大 `K(a)/K` で `[K(a):K]` が奇数ならば、`K(a²) = K(a)` を示せ。

---

## 第4章 分解体と代数閉包

### 学習目標

- 分解体の定義と、その存在・一意性を理解する。
- 代数閉体・代数閉包の概念を理解する。
- 具体的な多項式の分解体とその拡大次数を計算できる。

### 4.1 分解体の定義

**定義（分解体）** 体 K と多項式 f(X) ∈ K[X]（deg f ≥ 1）に対し、拡大体 L/K で次を満たすものを f の K 上の**分解体**と呼ぶ。

1. f は L 上で一次式の積に完全に分解する。
2. L は f の根全体で K 上生成される（最小性）。

**例 4.1** `f(X) = X³ − 2 ∈ Q[X]` の根は `∛2, ∛2ω, ∛2ω²`（ω は1の原始3乗根）。分解体は `L = Q(∛2, ω)` で、`[L:Q] = 6`。

### 4.2 分解体の存在と一意性

**定理（分解体の存在）** 任意の K[X] の多項式 f に対して、f の分解体が存在する。

**証明（骨子）** f の既約因子 g₁ を取る。`K₁ = K[X]/(g₁)` は体で、そこでは g₁ が根 α₁ を持つ。f を K₁ 上で因数分解し、同様の操作を根が全部現れるまで繰り返す。単拡大の構造定理により各ステップは正当化される。有限回で終わる。□

**定理（分解体の一意性）** f ∈ K[X] の2つの分解体 L₁, L₂ は K 上の同型を持つ（同型を除いて一意）。

**証明（骨子）** 既約因子ごとに「根の対応」を作る同型延長定理を反復適用する。K₁ ≅ K₂ の同型が f の根の対応によって延長されていくことが、単拡大の同型（3.3節）と帰納法で示される。□

**例 4.2** `X² + 1 ∈ Q[X]` の分解体は `Q(i)`，`[Q(i):Q] = 2`。

**例 4.3** `X⁴ − 5X² + 6 = (X²−2)(X²−3)` の分解体は `Q(√2, √3)`，次数 4。

### 4.3 代数閉体と代数閉包

**定義（代数閉体）** 体 K の任意の非定数多項式が K 上に根を持つとき、K を**代数閉体**と呼ぶ。体 K の代数拡大体 Ω が代数閉体であるとき、Ω を K の**代数閉包**と呼び、`K̄`（または K^alg）と書く。

**定理（代数閉包の存在と一意性）** 任意の体 K に対して代数閉包 K̄ が存在し、K 上の同型を除いて一意である。また K̄ は K 上代数的な元全体に一致する。

**例 4.4** C は代数閉体（代数学の基本定理）。`Q̄` は Q 上代数的な複素数全体（代数的数体）。

**注意** 分解体は代数閉包の内部に構成できる。具体的には、f の分解体は `K̄` 内の f の根で生成される体である。

### 4.4 分解体の次数計算

**例 4.5** `f(X) = X⁴ − 2 ∈ Q[X]` の分解体を求める。根は `2^(1/4), i·2^(1/4), −2^(1/4), −i·2^(1/4)`。分解体は `L = Q(2^(1/4), i)`。`[Q(2^(1/4)):Q] = 4`（最小多項式 X⁴−2 は既約）で、i ∉ Q(2^(1/4))（実体）だから `[L:Q] = 8`。

**例 4.6** `X^n − 1 ∈ Q[X]` の分解体は円分体 `Q(ζ_n)`（ζ_n は1の原始n乗根）。`[Q(ζ_n):Q] = φ(n)`（オイラーのφ関数）。証明は第7章の円分体の節で与える。

### 本章のまとめ

- 分解体は「すべての根を添加した最小の体」で、存在し同型を除いて一意。
- 代数閉包は代数拡大全体の行き先を与える普遍的な場。
- 分解体の次数は、根の添加の連鎖律で順に計算する。

### 練習問題

**練習問題 4.1** `X³ − 2` の Q 上の分解体の拡大次数を求めよ。

**練習問題 4.2** `X⁴ − 2` の Q 上の分解体の拡大次数を求めよ。

**練習問題 4.3** `X³ + X + 1 ∈ F_2[X]` が既約であることを示し、その分解体の元の個数を求めよ。

**練習問題 4.4** `f(X) = X³ − 1 ∈ Q[X]` の分解体を求めよ（1の原始3乗根 ω を用いて）。

**練習問題 4.5** 代数閉体は有限体でないことを示せ。（ヒント: 有限体 F で、1 + ∏_{a∈F}(X − a) を考える）

---

## 第5章 分離拡大と有限体

### 学習目標

- 分離多項式の判定（微分による）を理解する。
- 標数 0 の体ではすべての拡大が分離的であることを理解する。
- 完全体の概念を理解する。
- 有限体の構造（F_(p^n) の構成とガロア群）を理解する。

### 5.1 分離多項式と重根

**定義（分離多項式）** f(X) ∈ K[X] が、その分解体において**重根を持たない**とき、f は**分離的**であるという。

**命題（微分による判定）** f(X) が分離的であるための必要十分条件は

```
gcd(f(X), f'(X)) = 1
```

であること。ここで f' は形式微分。

**証明** α が f の分解体における重根 ⇔ α が f と f' の共通根（因数定理＋微分の定義による）。よって重根があれば gcd ≠ 1。逆も同様。□

**系** 標数 0 では、既約多項式は常に分離的である。

**証明** 既約 f が重根を持つなら f' と共通根を持ち、deg f' < deg f かつ f' ≠ 0 より gcd(f, f') が非自明な因子になり、f の既約性に矛盾。標数 0 では非定数多項式の微分は 0 でない。□

### 5.2 分離拡大

**定義（分離拡大）** 代数拡大 L/K で、L の**すべての元** a の最小多項式 irr(a, K) が分離的であるとき、分離拡大という。

**例 5.1** 標数 0 の体上の代数拡大はすべて分離拡大（上の系より）。

標数 p > 0 では分離的でない拡大が存在する。

**例 5.2** `K = F_p(t)`（有理関数体）、`L = K(t^(1/p))` を考える。`t^(1/p)` の最小多項式は `X^p − t`。分解体で `X^p − t = (X − t^(1/p))^p` となり重根を持つ。よって L/K は**非分離拡大**。ここで X^p − t の微分は pX^(p−1) = 0（標数 p）。

**定義（純非分離拡大）** 代数拡大 L/K で、すべての元の最小多項式が `X^(p^n) − a` の形（重根を持つ）のとき、純非分離拡大という。任意の代数拡大は分離拡大と純非分離拡大の塔として分解できる（基本的な分解定理）。

### 5.3 完全体

**定義（完全体）** 体 K が次の同値な条件を満たすとき、**完全体**と呼ぶ。

1. K 上のすべての既約多項式が分離的である。
2. 標数 0、または標数 p でフロベニウス写像 `φ: x ↦ x^p` が全射。

**例 5.3** 標数 0 の体（Q, R, C）はすべて完全体。有限体はすべて完全体（フロベニウス写像は単射、有限なら全射）。`F_p(t)` は完全体でない。

### 5.4 有限体の構成

**定理（有限体の構造）** p を素数、n ≥ 1 とする。`x^(p^n) − x` の F_p 上の分解体は `p^n` 個の元からなる体 `F_(p^n)` であり、逆に任意の有限体はある `F_(p^n)` に同型である。また `F_(p^m) ⊂ F_(p^n)` が成り立つのは `m | n` のときかつそのときに限る。

**証明の骨子** `S = { a ∈ K̄ | a^(p^n) = a }` は部分体である（フロベニウスの冪が準同型であることから）。`X^(p^n) − X` は微分が −1 で分離的、S はちょうどその根全体で |S| = p^n。逆に、位数 p^n の有限体 F では F* が位数 p^n − 1 の乗法群だから、任意の a ∈ F* で a^(p^n−1) = 1、よって a^(p^n) = a。0 でも成り立つから、F の元はすべて X^(p^n) − X の根。従って F ⊂ F_(p^n) かつ個数が等しいので一致。□

### 5.5 有限体のガロア拡大とフロベニウス写像

**定理** `F_(p^n)/F_p` のガロア群は位数 n の巡回群で、**フロベニウス写像** `φ: x ↦ x^p` が生成元である。

**証明の骨子** `F_(p^n)` の元のうち `x^p = x` を満たすのは F_p の元のみ。よって固定体は F_p。後述するアルティンの定理により、`|Gal(F_(p^n)/F_p)| = [F_(p^n):F_p] = n`。φ は位数 n の自己同型（x^(p^n) = x より φ^n = id、x^(p^k) = x の解は F_(p^k) の元のみで、k < n なら真部分体だから φ^k ≠ id）。よって Gal = ⟨φ⟩。□

### 本章のまとめ

- 分離的 ⇔ gcd(f, f') = 1。標数 0 では既約多項式はすべて分離的。
- 完全体 = すべての既約多項式が分離的（標数 p ではフロベニウスの全射性）。
- 有限体は `F_(p^n)` で、ガロア群はフロベニウス写像が生成する巡回群。
- 非分離拡大は標数 p > 0 の特殊事情（例：`F_p(t^(1/p))/F_p(t)`）。

### 練習問題

**練習問題 5.1** `f(X) = X³ + X + 1 ∈ F_2[X]` が分離的であることを示せ。

**練習問題 5.2** `X^p − t ∈ F_p(t)[X]`（p素数）の既約性と分離性を判定せよ。

**練習問題 5.3** `F_4` の乗法群の構造を説明し、`F_4 = F_2[X]/(X²+X+1)` の元をすべて書き下せ。

**練習問題 5.4** `F_8 = F_2[X]/(X³+X+1)` とする。フロベニウス写像の位数を求め、`Gal(F_8/F_2)` の構造を答えよ。

**練習問題 5.5** 有限体 `F_q` の任意の元 a について `a^q = a` となることを示せ。

---

## 第6章 正規拡大

### 学習目標

- 正規拡大の定義と同値条件を理解する。
- 正規拡大の例・非正規拡大の例を挙げられる。
- 正規閉包の概念を理解する。

### 6.1 正規拡大の定義

**定義（正規拡大）** 代数拡大 L/K が次の条件を満たすとき、**正規拡大**と呼ぶ。

> K 上既約な多項式 f ∈ K[X] が L に根を一つ持てば、f は L 上で一次式の積に完全に分解する。

直感的には「既約多項式の根の1つを取り込んだら、そのすべての共役も取り込まれている」ことを意味する。

### 6.2 正規拡大の同値条件

**定理（正規拡大の同値条件）** 有限次代数拡大 L/K に対して、次は同値。

1. L/K は正規拡大。
2. ある f ∈ K[X] の分解体になっている（L が K 上のある多項式の分解体）。
3. K 上の同型写像 `σ: L → K̄` を任意に取ると、`σ(L) = L` が成り立つ。

**証明（3 ⇒ 1）** f ∈ K[X] 既約で、根 α ∈ L を持つとする。f の別の根 β ∈ K̄ を取る。単拡大の同型定理（3.3節）より `K(α) ≅ K(β)`（α ↦ β）が延長されて同型 `σ: L → K̄` が存在する（同型延長定理）。条件3より σ(L) = L、よって β = σ(α) ∈ L。すなわちすべての根は L に含まれる。□

**1 ⇒ 2** L = K(a₁, …, aₙ)（有限生成）とする。各 a_i の最小多項式 f_i の積 f の分解体は、正規性により L に含まれ、かつ L の生成元を含むから L に一致する。□

**2 ⇒ 3** L を f の分解体とし、σ: L → K̄ を同型とする。σ は f の根を f の根に写す。L は f の根で生成されるから σ(L) ⊂ L。両辺の次数が等しい（σ は単射なので [σ(L):K] = [L:K]）から σ(L) = L。□

**例 6.1** `Q(√2)/Q` は正規拡大（X²−2 の分解体）。

**例 6.2** `Q(∛2)/Q` は正規拡大でない。X³−2 は既約で、∛2 ∈ Q(∛2) だが他の根 `∛2ω, ∛2ω²` は実数でないから Q(∛2) に含まれない。

### 6.3 正規閉包

**定義（正規閉包）** 代数拡大 L/K に対して、L を含む K の代数拡大 N で、N/K が正規であり、かつ L/K の「最小の」正規拡大であるものを L の K 上の**正規閉包**と呼ぶ。具体的には、L の K 上の生成元 a₁, …, aₙ の各最小多項式の積の分解体として得られる。

**例 6.3** `Q(∛2)/Q` の正規閉包は `Q(∛2, ω)`（X³−2 の分解体）。

### 本章のまとめ

- 正規拡大は「既約多項式の1根があれば全根ある」拡大。
- 有限次正規拡大はある多項式の分解体と同値。
- `Q(∛2)/Q` が正規でないのが典型的な反例。
- 正規閉包は最小多項式の積の分解体として常に存在する。

### 練習問題

**練習問題 6.1** `Q(√2, √3)/Q` が正規拡大であることを示せ。

**練習問題 6.2** `Q(√2)/Q` は正規拡大であるが、`Q(√2)/Q` の部分拡大 `Q/ Q` を考え、なぜ `Q(√2)` が正規で `Q(∛2)` が正規でないのかを根の共役の観点から説明せよ。

**練習問題 6.3** `Q(2^(1/4))/Q` は正規拡大か。判定せよ。

**練習問題 6.4** `Q(∛2)/Q` の正規閉包を求めよ。

**練習問題 6.5** 有限次拡大 L/K が正規拡大ならば、任意の既約多項式 f ∈ K[X] に対して、f が L に根を持てば f は L 上で一次式の積に分解することを示せ。

---

## 第7章 ガロア拡大とガロア群

### 学習目標

- ガロア群 Gal(L/K) の定義を理解する。
- ガロア拡大の定義（正規かつ分離、次数＝ガロア群の位数）を理解する。
- 固定体の概念とアルティンの定理を理解する。
- 2次・3次拡大、円分体のガロア群を計算できる。

### 7.1 ガロア群の定義

**定義（K 自己同型）** 体 L の自己同型 σ: L → L で、K のすべての元を固定するもの（σ(k) = k for all k ∈ K）を L の **K 自己同型**と呼ぶ。K 自己同型全体の集合は写像の合成に関して群になる。

**定義（ガロア群）** L/K を体拡大とするとき、L の K 自己同型全体のなす群を**ガロア群**と呼び、`Gal(L/K)` と書く。

**例 7.1** `Gal(Q(√2)/Q) = { id, σ }`。ここで σ は `√2 ↦ −√2`（および Q の元を固定）で定義される。位数 2 の群なので `Gal(Q(√2)/Q) ≅ Z/2Z`。

**例 7.2** `Gal(C/R) = { id, 複素共役 } ≅ Z/2Z`。

### 7.2 固定体

**定義（固定体）** Gal(L/K) の部分群 H に対して、H のすべての元で固定される L の元の集合

```
L^H = { x ∈ L | σ(x) = x for all σ ∈ H }
```

は L の部分体になる。これを H の**固定体**と呼ぶ。特に H = Gal(L/K) のとき、`L^H = K` であるかどうかが重要になる。

### 7.3 ガロア拡大の定義

**定義（ガロア拡大）** 代数拡大 L/K が**正規かつ分離**であるとき、ガロア拡大と呼ぶ。

次の定理が、次数とガロア群の位数を結びつける。

**定理（アルティンの定理）** 体 L の自己同型の有限群 G に対し、`[L : L^G] = |G|` が成り立つ。

**証明（骨子）** `G = { σ₁ = id, σ₂, …, σₙ }`，K = L^G とする。任意の x ∈ L を取る。`x, σ₂(x), …, σₙ(x)` を係数とする `f_X(T) = ∏_{σ∈G}(T − σ(x))` を考える。これは係数が K に属する多項式で、x はその根。よって x は K 上代数的で、次数 ≤ n。ここから `[L:K] ≤ n` が出る。一方、一次独立性の議論（線型方程式を解く）で `[L:K] ≥ n` も示され、結局 `[L:K] = n`。□

**系（ガロア拡大の基本性質）** L/K が有限次ガロア拡大のとき

```
[L:K] = |Gal(L/K)|
```

が成り立つ。また `L^(Gal(L/K)) = K`。

**例 7.3** `L = Q(√2, √3)`，`Gal(L/Q)` の元は、√2 ↦ ±√2，√3 ↦ ±√3 の組み合わせで4つ。よって `Gal(L/Q) ≅ Z/2Z × Z/2Z`（クラインの四元群）で、`[L:Q] = 4` と一致。

### 7.4 円分体と巡回群

**定理（円分体のガロア群）** `ζ_n` を 1 の原始 n 乗根とする。円分体 `Q(ζ_n)/Q` はガロア拡大で、

```
Gal(Q(ζ_n)/Q) ≅ (Z/nZ)*
```

が成り立つ。特に `[Q(ζ_n):Q] = φ(n)`。

**証明の骨子** `X^n − 1` の分解体だから正規。分離的（標数0）だからガロア。自己同型 σ は ζ_n を 1 の n 乗根に写す。σ(ζ_n) が原始 n 乗根であることから `σ(ζ_n) = ζ_n^k`（gcd(k, n) = 1）と表せ、対応 `σ ↦ k mod n` が群同型 `Gal → (Z/nZ)*` を与える。単射性は生成元の行き先で決まることから、全射性と位数 φ(n) が従う。□

**例 7.4** `Gal(Q(ζ_3)/Q) ≅ (Z/3Z)* ≅ Z/2Z`。`Q(ζ_3) = Q(√−3)` とも書ける。

**例 7.5** `Gal(Q(ζ_5)/Q) ≅ (Z/5Z)* ≅ Z/4Z`（巡回群）。

**例 7.6** `Gal(Q(ζ_8)/Q) ≅ (Z/8Z)* ≅ Z/2Z × Z/2Z`（非巡回）。

### 7.5 有限体のガロア群（再掲）

第5章で見た通り、`F_(p^n)/F_p` はガロア拡大で `Gal(F_(p^n)/F_p) ≅ Z/nZ`（フロベニウス写像が生成）。

### 本章のまとめ

- ガロア群は K を固定する L の自己同型群。
- ガロア拡大（正規かつ分離）では `[L:K] = |Gal(L/K)|`，固定体は K。
- アルティンの定理はこの次数一致の核心。
- 円分体のガロア群は `(Z/nZ)*`。有限体では巡回群。

### 練習問題

**練習問題 7.1** `Gal(Q(√2, √3)/Q)` の元を具体的に書き、`[Q(√2,√3):Q]` と比較せよ。

**練習問題 7.2** `Gal(Q(√2, √3, √5)/Q)` の位数と構造を求めよ。

**練習問題 7.3** `Gal(Q(ζ_5)/Q)` の生成元を求め、位数を確認せよ。

**練習問題 7.4** `Gal(F_16/F_2)` の構造を求めよ。

**練習問題 7.5** 有限次拡大 L/K がガロア拡大であるための必要十分条件を述べよ。また、`Q(∛2)/Q` がガロア拡大でないことを示せ。

---

## 第8章 ガロア理論の基本定理

### 学習目標

- ガロア理論の基本定理（中間体と部分群の対応）を正確に述べられる。
- 対応が順序を反転すること、中間体と部分群の拡大次数の関係を理解する。
- 基本定理を具体的な例（Q(√2,√3) など）で使いこなせる。

### 8.1 基本定理の主張

**定理（ガロア理論の基本定理）** `L/K` を有限次ガロア拡大，G = Gal(L/K) とする。このとき次の2つの写像

```
Φ: { 中間体 F | K ⊂ F ⊂ L }  →  { 部分群 H | H ≤ G }
   F  ↦  Gal(L/F)

Ψ: { 部分群 H | H ≤ G }  →  { 中間体 F | K ⊂ F ⊂ L }
   H  ↦  L^H
```

は互いに逆写像であり、**全単射**を与える。さらに次が成り立つ。

1. **順序反転**: `F₁ ⊂ F₂ ⇔ Gal(L/F₁) ⊃ Gal(L/F₂)`。
2. **次数の対応**: `[L:F] = |Gal(L/F)|`，`[F:K] = [G : Gal(L/F)]`（指数）。
3. F が K 上のガロア拡大であるための必要十分条件は `Gal(L/F) ⊴ G`（正規部分群）であること。そのとき `Gal(F/K) ≅ G / Gal(L/F)`。

つまり「中間体」と「ガロア群の部分群」は一対一に対応し、大きい中間体ほど小さい部分群に対応する。

### 8.2 基本定理の証明

**証明の流れ** 次の3つの性質から従う。

**(a) 単調性**: `F₁ ⊂ F₂ ⇒ Gal(L/F₁) ⊃ Gal(L/F₂)`（定義から明らか）。逆に `H₁ ⊂ H₂ ⇒ L^(H₁) ⊃ L^(H₂)`（定義から明らか）。

**(b) 合成が恒等になること**: `F ⊂ L^(Gal(L/F))` は常に成り立つ。ガロア拡大 L/F はガロア拡大だから、第7章の系より `L^(Gal(L/F)) = F`。

**(c) 逆方向の合成**: `H ⊂ Gal(L/L^H)` は常に成り立つ。アルティンの定理を L と H に適用すると `[L:L^H] = |H|`。一方、L/L^H はガロア拡大だから `[L:L^H] = |Gal(L/L^H)|`。よって `|H| = |Gal(L/L^H)|` であり、H ⊂ Gal(L/L^H) と合わせて `H = Gal(L/L^H)`。

以上より Φ と Ψ は互いに逆。次数の対応はアルティンの定理（あるいはガロア拡大の次数公式）から従う。正規部分群の対応は、剰余群による作用の議論で示される（詳細は発展）。□

### 8.3 例：Q(√2, √3)

`L = Q(√2, √3)`，`G = Gal(L/Q) ≅ Z/2Z × Z/2Z = { id, σ, τ, στ }`。ここで σ は √2 ↦ −√2，τ は √3 ↦ −√3 と定義する。

G の部分群と中間体の対応は次の通り。

| 部分群 H | 位数 | 中間体 L^H | [中間体:Q] |
|---|---|---|---|
| {id} | 1 | Q(√2, √3) | 4 |
| {id, σ} | 2 | Q(√3) | 2 |
| {id, τ} | 2 | Q(√2) | 2 |
| {id, στ} | 2 | Q(√6) | 2 |
| G | 4 | Q | 1 |

中間体のうち、ガロア拡大に対応するのは正規部分群 {id}, G のみで、対応する中間体 Q(√2,√3) と Q が Q 上ガロア拡大である。2次の中間体 Q(√2), Q(√3), Q(√6) のうち Q 上正規なものは「√2 の共役 −√2 ∈ Q(√2)」なので Q(√2) は正規、Q(√6) も正規。実は Z/2Z×Z/2Z は可換群で、すべての部分群が正規なので、実は Q(√3), Q(√2), Q(√6) はすべて Q 上ガロア拡大である（2次拡大は自動的にガロア拡大）。

### 8.4 ガロア対応の使い方

ガロア理論の基本定理の威力は、**体の問題を群の問題に翻訳できる**点にある。

- 「中間体をすべて求めよ」→「ガロア群の部分群をすべて求めよ」（部分群は計算しやすい）。
- 「中間体が何個あるか」→「部分群の個数」。
- 「その中間体がガロア拡大かどうか」→「対応する部分群が正規部分群かどうか」。

**例 8.1** `L = Q(∛2, ω)`（X³−2 の分解体）。`Gal(L/Q) ≅ S₃`（位数6）。S₃ の部分群は {id}，位数2の部分群（3個），位数3の部分群 A₃（1個），S₃ 自身。よって中間体は 6 個で、2次の中間体が3個（それぞれ Q(ω)，Q(∛2·ω + 共役) など）、3次の中間体が1個（Q(ω)）となる。

### 本章のまとめ

- ガロア理論の基本定理: 中間体 ⇔ ガロア群の部分群 の全単射（順序反転）。
- `[L:F] = |Gal(L/F)|`，`[F:K] = [G : Gal(L/F)]`。
- F/K がガロア拡大 ⇔ Gal(L/F) ⊴ G。そのとき Gal(F/K) ≅ G/Gal(L/F)。
- 証明の核心はアルティンの定理とガロア拡大の次数公式。

### 練習問題

**練習問題 8.1** `L = Q(√2, √3, √5)` のガロア群の構造を求め、部分群と中間体の対応をすべて書き下せ。

**練習問題 8.2** `Gal(Q(∛2, ω)/Q) ≅ S₃` を用いて、Q(∛2, ω) の中間体をすべて求めよ。

**練習問題 8.3** ガロア理論の基本定理を述べよ。

**練習問題 8.4** `Q(√2, √3)/Q` の部分拡大 `Q(√6)` に対応する部分群を求め、その部分群が正規であることを示せ。

**練習問題 8.5** `L/K` を有限次ガロア拡大とする。中間体 F が `[F:K] = 2` を満たすとき、Gal(L/F) は G の正規部分群で指数 2 であることを示せ。

---

## 第9章 応用：作図問題と方程式の可解性

### 学習目標

- 定規とコンパスによる作図可能性を体拡大の問題に翻訳できる。
- 有名な作図不可能問題（立方体倍積・角の3等分・円積）を説明できる。
- 冪根拡大と可解群の定義を理解する。
- 「冪根で解ける ⇔ ガロア群が可解群」を理解する。
- **一般5次方程式が四則演算と冪根だけでは解けないことを証明できる**。

### 9.1 作図問題と体の理論

平面を複素平面と同一視する。**作図可能な数**を次のように再帰的に定義する。

**定義（作図可能な数）** 複素数 z が次を満たすとき、z は（0と1から始めて定規とコンパスで）**作図可能**であるという。

- 2点 0, 1 から始めて、定規（直線を引く）とコンパス（円を描く）の操作を有限回繰り返して得られる交点の座標として表される。

**定理（作図可能性と体の対応）** 作図可能な複素数全体の集合 F は C の部分体であり、作図可能な数 z に対して `[Q(z):Q]` は **2 の冪**になる。より精密には、z が作図可能ならば、体の列

```
Q = F₀ ⊂ F₁ ⊂ ⋯ ⊂ F_m
```

で `[F_i : F_{i-1}] = 2`（各拡大は2次拡大）かつ `z ∈ F_m` となるものが存在する。

**証明の骨子** 作図操作は「2つの直線の交点」「直線と円の交点」「2つの円の交点」に帰着し、いずれも既知の作図可能数と係数が Q の方程式の解として表され、座標は2次方程式の解として得られる。新たに得られる点は高々2次の拡大で生成され、この操作を繰り返す。□

### 9.2 作図不可能な古典的問題

**定理（立方体倍積の不可能性）** 与えられた立方体の2倍の体積を持つ立方体の辺の長さは作図できない。

**証明** 体積を2倍にするには辺を `∛2` 倍する必要がある。もし `∛2` が作図可能なら `[Q(∛2):Q]` は2の冪でなければならないが、実際は `[Q(∛2):Q] = 3` で、3 は 2 の冪でない。矛盾。□

**定理（角の3等分の不可能性）** 一般の角は定規とコンパスでは3等分できない。特に 60° の角は3等分できない。

**証明** 60° の3等分は cos20° の作図に帰着する。`cos3θ = 4cos³θ − 3cosθ` に θ=20° を代入すると `4x³ − 3x = 1/2`，すなわち `8x³ − 6x − 1 = 0`。この3次多項式は Q 上既約（有理根 1/2 などをチェックするが、有理根の候補 ±1, ±1/2, ±1/4, ±1/8 のどれも根でない）で、`[Q(cos20°):Q] = 3`。2の冪でないので作図不可能。□

**定理（円積問題）** 半径1の円と同じ面積の正方形を作図することはできない。

**証明** 面積 π の正方形の辺は `√π`。もし作図可能なら √π は代数的数だが、π は超越数（リンデマンの定理）なので √π も超越数。代数的でないから作図可能でない。□

### 9.3 冪根拡大

方程式が「四則演算と冪根だけで解ける」ことを、体拡大の言葉で正確に定義する。

**定義（冪根拡大）** 体の列

```
K = K₀ ⊂ K₁ ⊂ ⋯ ⊂ K_m = L
```

が存在し、各 i について `K_i = K_{i-1}(α_i)` で、ある自然数 n_i に対して `α_i^(n_i) ∈ K_{i-1}` が成り立つとき、L は K 上**冪根拡大**（根号拡大）であるという。つまり各段階で「ある数の n 乗根」を添加している。

**定義（冪根で解ける方程式）** f(X) ∈ K[X] の分解体 L が、K の上のある冪根拡大に含まれるとき、f は K 上**冪根で解ける**（代数的に解ける）という。

**注意** ここで「冪根で解ける」は「解の公式が四則演算と n 乗根（√, ∛, …）だけで書ける」ことの定式化である。

### 9.4 可解群

**定義（可解群）** 有限群 G が次の条件を満たすとき、**可解群**と呼ぶ。

> 部分群の列（正規列）
> ```
> {e} = G₀ ⊂ G₁ ⊂ ⋯ ⊂ G_m = G
> ```
> が存在して、各 i について `G_{i-1} ⊴ G_i` かつ剰余群 `G_i / G_{i-1}` が**アーベル群**である。

**例 9.1**
- すべてのアーベル群は可解群。
- S₃ は可解群：`{e} ⊴ A₃ ⊴ S₃`，剰余は A₃ ≅ Z/3Z，S₃/A₃ ≅ Z/2Z（いずれもアーベル）。
- S₄ は可解群（`{e} ⊴ V₄ ⊴ A₄ ⊴ S₄` など）。

**命題（可解群の性質）** 可解群の部分群・剰余群は可解群。群 G が可解群 ⇔ 正規部分群 N ⊴ G について N と G/N がともに可解群。

### 9.5 ガロア群と冪根可解性

**定理（冪根拡大とガロア群）** K を標数 0 の体とする。既約多項式 f ∈ K[X] の分解体を L とする。このとき

```
f が冪根で解ける ⇔ Gal(L/K) が可解群
```

**証明（骨子）**
（⇒）L が K の冪根拡大 M に含まれるとする。M を正規閉包で取り直し、L ⊂ M、M/K がガロア拡大としてよい。ガロア理論の基本定理により M の中間体の列は Gal(M/K) の部分群の列に対応する。冪根拡大の各段階は「1の冪根を添加（巡回群）」「n乗根を添加（可換群）」の組み合わせで、対応する剰余群がアーベル群になる。よって Gal(M/K) は可解群。部分群 Gal(L/K) ≅ Gal(M/K)/Gal(M/L) も可解群（可解群の部分群・剰余群は可解群）。
（⇐）逆に Gal(L/K) が可解群なら、正規列の各段階のアーベル剰余群に対応して、1の冪根と n 乗根の添加によって中間体の塔を実現できる（ラグランジュの分解式による古典的構成）。よって冪根で解ける。□

**例 9.2** `f(X) = X³ − 2 ∈ Q[X]`。分解体のガロア群は S₃。S₃ は可解群なので、X³−2 は冪根で解ける。実際、3次方程式はカルダノの公式で解ける。

**例 9.3** `f(X) = X² − 2`。ガロア群は Z/2Z（可解）。解は ±√2。

### 9.6 対称群の可解性

**定理（Sₙ の可解性）** n ≥ 5 に対して、n次対称群 Sₙ は**可解群ではない**。

**証明の骨子** 交代群 Aₙ（n ≥ 5）が**単純群**であること（交代群の単純性）を認める。Aₙ ⊴ Sₙ で、Sₙ が可解群なら Aₙ も可解群（可解群の部分群は可解群）。しかし単純群 Aₙ（位数 > 1）が可解群だとすると、正規列 `{e} ⊂ G₁ ⊂ ⋯ ⊂ Aₙ` の最初の非自明な正規部分群 G₁ がアーベル剰余群を持つことになるが、Aₙ は単純だから G₁ = Aₙ となり、Aₙ 自身がアーベルでなければならない。しかし n ≥ 4 で Aₙ は非アーベル（3-サイクル同士の交換が恒等でない）。矛盾。よって Sₙ（n ≥ 5）は非可解。□

特に S₅ は非可解である。これは以下で一般5次方程式の可解性を否定する鍵となる。

### 9.7 一般5次方程式のガロア群は S₅

**定義（一般 n 次方程式）** `x₁, …, xₙ` を不定元とし、体 `K₀ = Q(x₁, …, xₙ)` を考え、`F = Q(e₁, …, eₙ)` を基本対称式 e_i で生成される部分体とする。ここで

```
e₁ = x₁ + ⋯ + xₙ
e₂ = ∑_{i<j} x_i x_j
⋮
eₙ = x₁ x₂ ⋯ xₙ
```

である。このとき

```
f(T) = ∏_{i=1}^{n} (T − x_i)
     = Tⁿ − e₁Tⁿ⁻¹ + e₂Tⁿ⁻² − ⋯ + (−1)ⁿeₙ
```

は F 上の多項式である。これを F 上の**一般 n 次方程式**と呼ぶ。その解（根）は x₁, …, xₙ である。

**定理（一般 n 次方程式のガロア群）** 一般 n 次方程式 f(T) ∈ F[T] の分解体は K₀ = F(x₁, …, xₙ) であり、ガロア群は

```
Gal(K₀/F) ≅ Sₙ
```

が成り立つ（Sₙ は x₁, …, xₙ の置換によって作用する）。

**証明の骨子** 対称式の基本定理により、`K₀^(Sₙ) = F`（Sₙ で不変な有理関数は基本対称式の有理関数）。したがってアルティンの定理（あるいはガロア拡大の定義）より `Gal(K₀/F) = Sₙ`。□

**注意** この定理は「文字係数の一般 n 次方程式のガロア群は Sₙ」という事実を確立する。特殊な n 次方程式（係数に特別な関係があるもの）はガロア群が Sₙ より小さくなり得るが、**一般の** n 次方程式ではちょうど Sₙ になる。

### 9.8 アーベル–ルフィニの定理：5次方程式の代数解法の不存在

**定理（アーベル–ルフィニ）** 一般の5次以上の代数方程式は、四則演算と冪根のみでは解くことができない。

**証明** n ≥ 5 とする。一般 n 次方程式 f(T) ∈ F[T] を考える。9.7節の定理により `Gal(K₀/F) ≅ Sₙ`。n ≥ 5 だから、9.6節の定理により Sₙ は**可解群ではない**。よって 9.5節の定理（冪根可解性 ⇔ 可解群）により、一般 n 次方程式は**冪根で解けない**。□

**この証明のエッセンス** ガロア理論の力は、この命題を「ガロア群 Sₙ が可解でない」という群論の事実に帰着させたところにある。方程式の複雑さは、根の入れ替え（対称性）の複雑さとして現れ、それを測るのがガロア群なのである。

**例 9.4（実例）** 具体的な「解けない5次方程式」の例として `X⁵ − X − 1 ∈ Q[X]` がある。この5次多項式のガロア群は S₅ であり、したがってその根は四則演算と冪根だけでは表せない。

**補足（歴史的ノート）** アーベル（Niels Henrik Abel, 1824）が一般5次方程式が冪根で解けないことを代数的手法で示し、ガロア（1831年頃）がガロア群の考えで統一的な理論を与えた。ガロアが測ったのは「方程式の対称性」である。

### 本章のまとめ

- 作図可能な数 z は `[Q(z):Q]` が2の冪。立方体倍積（∛2）、角の3等分（3次既約）、円積（超越数）は作図不可能。
- 冪根で解ける ⇔ ガロア群が可解群。
- n ≥ 5 で Sₙ は非可解（Aₙ の単純性）。
- 一般 n 次方程式のガロア群は Sₙ。よって一般5次以上は冪根で解けない（アーベル–ルフィニ）。
- これが本書のゴールである。

### 練習問題

**練習問題 9.1** `∛2` が作図可能でないことを、体拡大の次数を用いて示せ。

**練習問題 9.2** S₃ が可解群であることを正規列を明示して示せ。

**練習問題 9.3** 交代群 A₄ は可解群であることを示せ。（ヒント: `V₄ = { id, (12)(34), (13)(24), (14)(23) }` が正規部分群）

**練習問題 9.4** 一般4次方程式のガロア群 S₄ は可解群であり、したがって4次方程式は冪根で解けることを説明せよ。

**練習問題 9.5** アーベル–ルフィニの定理を、ガロア理論の言葉（ガロア群、可解群、S₅）を用いて説明せよ。

---

## 第10章 総まとめと演習

### 学習目標

- 第1〜9章の流れを俯瞰し、概念同士のつながりを再確認する。
- 院試レベルの総合演習を解いて、総合的な理解を確認する。

### 10.1 全体の流れ

本書の論理の流れは以下のようになっている。

```
群・環・体の基礎（第1章）
      ↓
体の拡大と拡大次数（第2章）
      ↓
代数拡大・最小多項式・単拡大（第3章）
      ↓
分解体・代数閉包（第4章）
      ↓
分離拡大・有限体（第5章）
      ↓
正規拡大（第6章）
      ↓
ガロア拡大・ガロア群（第7章）   ← 正規＋分離，[L:K] = |Gal(L/K)|
      ↓
ガロア理論の基本定理（第8章）   ← 中間体 ⇔ 部分群
      ↓
作図問題・方程式の可解性（第9章）← 5次方程式の代数解法の不存在
```

### 10.2 重要定理の一覧

- **ラグランジュの定理**: 有限群 G と部分群 H で |G| = |H|·[G:H]。
- **準同型定理**: G₁/Ker f ≅ Im f。
- **単拡大の構造**: K(a) ≅ K[X]/(irr(a,K))，[K(a):K] = deg irr(a,K)。
- **分解体の存在と一意性**: 任意の f ∈ K[X] に分解体が存在し、同型を除いて一意。
- **アルティンの定理**: [L : L^G] = |G|。
- **ガロア拡大の基本性質**: [L:K] = |Gal(L/K)|。
- **ガロア理論の基本定理**: 中間体 ⇔ 部分群（順序反転）。F/K ガロア ⇔ Gal(L/F) ⊴ G。
- **円分体**: Gal(Q(ζ_n)/Q) ≅ (Z/nZ)*。
- **有限体**: Gal(F_(p^n)/F_p) ≅ Z/nZ。
- **冪根可解性定理**: f が冪根で解ける ⇔ Gal(分解体/K) が可解群。
- **Sₙ（n≥5）の非可解性**: Aₙ の単純性より。
- **アーベル–ルフィニ**: 一般5次以上は冪根で解けない。

### 10.3 総合演習

**演習 1** `L = Q(√2, √3)` のガロア群を求め、その部分群の個数を求めよ。また中間体をすべて挙げよ。

**演習 2** `F_8` を `F_2[X]/(X³+X+1)` と表したとき、`Gal(F_8/F_2)` の生成元と位数を求めよ。

**演習 3** `f(X) = X⁴ + X + 1 ∈ F_2[X]` の既約性と分離性を調べよ。

**演習 4** `Q(ζ_7)/Q` のガロア群の構造を求めよ（(Z/7Z)* の構造を調べる）。

**演習 5** 正規拡大ではない有限次代数拡大の例を挙げ、なぜ正規でないかを説明せよ。

**演習 6** 「作図可能な数 z に対して [Q(z):Q] は 2 の冪」という定理を述べ、これを立方体倍積問題に適用せよ。

**演習 7** 一般の5次方程式が冪根で解けないことを示す手順を、用いる定理（ガロア理論の基本定理、冪根可解性定理、S₅ の非可解性）に分けて説明せよ。

**演習 8** 位数 8 の群は可解群であることを説明せよ。（ヒント: 有限p群は可解群である）

**演習 9** K を標数 0 の体とする。`X^p − a ∈ K[X]`（p 素数）が既約なら、その分解体のガロア群は巡回群であることを示せ。

**演習 10** ガロア理論の基本定理を正確に述べ、その証明の要点を3点に整理せよ。

---

## 付録A 練習問題の解答

### 第1章

**練習問題 1.1** 解答: ラグランジュの定理は「有限群 G と部分群 H に対し |G| = |H|·[G:H]」。位数6の群の部分群の位数は 1, 2, 3, 6（6の約数）。

**練習問題 1.2** 解答: Ker f = { n ∈ Z | n ≡ 0 mod 4 } = 4Z。準同型定理より Z/4Z ≅ Im f = Z/4Z（f は全射）。

**練習問題 1.3** 解答: Z/nZ が体になるのは n が素数のとき。すなわち nZ が極大イデアル ⇔ n は素数。

**練習問題 1.4** 解答: `(ab)^p = a^p b^p` は二項定理と、標数 p より中間の係数 pCi が 0 になることから従う。`(a+b)^p = a^p + b^p` も同様。加法・乗法・単位元の対応を確かめれば体準同型。

**練習問題 1.5** 解答: 標数 2 なら `1 + 1 = 0`。任意の a で `a + a = a(1+1) = 0`。よって `(a+b)² = a² + ab + ba + b² = a² + 2ab + b² = a² + 0 + b²`（ab=ba は可換）。

### 第2章

**練習問題 2.1** 解答: `[Q(√2):Q] = 2`。Q(√2) = { a + b√2 | a,b ∈ Q } だから {1, √2} は生成系。a + b√2 = 0 とすると b = 0 なら a = 0；b ≠ 0 なら √2 = −a/b ∈ Q となり矛盾。よって一次独立で基底。

**練習問題 2.2** 解答: `[Q(√2):Q] = 2`，`[Q(√2,√3):Q(√2)] = 2`（√3 ∉ Q(√2)），`[Q(√2,√3,√5):Q(√2,√3)] = 2`（√5 ∉ Q(√2,√3)）。よって連鎖律で 2·2·2 = 8。

**練習問題 2.3** 解答: `[K(a):K] = n < ∞` ならば、1, a, …, aⁿ は n+1 個の元で一次従属。よって `c₀ + c₁a + ⋯ + cₙaⁿ = 0`（0でない係数）が存在し、a は代数的。

**練習問題 2.4** 解答: K(a), K(b) は代数拡大。K(a) の K 上の有限生成性と連鎖律より K(a,b) は K 上有限次拡大。有限次拡大は代数拡大だから a+b, ab ∈ K(a,b) は K 上代数的。

**練習問題 2.5** 解答: Q(√2) は代数拡大（√2 は代数的、Q(√2) の任意の元 a+b√2 は X²−2aX+(a²−2b²) の根）。R/Q は π ∈ R が超越的だから代数拡大でない。

### 第3章

**練習問題 3.1** 解答: irr(√5, Q) = X²−5。`[Q(√5):Q] = 2`。

**練習問題 3.2** 解答: `a = √2 + √3` とする。`(a²−5)² = (2√6)² = 24` より `a⁴ − 10a² + 1 = 0`。最小多項式は X⁴ − 10X² + 1（既約。もし2次以下の既約因子があれば Q(√2) か Q(√3) か Q(√6) に a が含まれねばならないが、a ∉ Q(√2), Q(√3) を確認できる）。`[Q(a):Q] = 4`。

**練習問題 3.3** 解答: f(X) = X²+X+1 は F_2 上で f(0) = 1, f(1) = 1 だから根を持たず、2次なので既約。K[X]/(f) は体で、元は {0, 1, x, x+1}（x = X mod f）の4個。x² = x+1 に注意（f(x)=0 より）。

**練習問題 3.4** 解答: `a = ∛2` とおく（a³ = 2）。`u = a² + a` の逆元を `v = b + ca + da²`（b, c, d ∈ Q）とおく。`a² = a³/a = 2/a` より `a³ = 2, a⁴ = 2a` を使って展開すると
`u·v = (2c + 2d) + (b + 2d)a + (b + c)a²`。これが 1 になるように連立方程式 `b + 2d = 0, b + c = 0, 2c + 2d = 1` を解くと `b = −1/3, c = 1/3, d = 1/6`。よって逆元は `−1/3 + (1/3)a + (1/6)a² = (a² + 2a − 2)/6`。

**練習問題 3.5** 解答: `[K(a):K]` を奇数 n とする。a² ∈ K(a) だから K(a²) ⊂ K(a)。`[K(a):K(a²)] ≤ 2`（a は X² − a² の根）。よって `n = [K(a):K(a²)]·[K(a²):K]` で n は奇数なので `[K(a):K(a²)]` は奇数、よって 1。したがって K(a) = K(a²)。

### 第4章

**練習問題 4.1** 解答: 根は ∛2, ∛2ω, ∛2ω²。分解体 L = Q(∛2, ω)。`[Q(∛2):Q] = 3`。ω は X²+X+1 の根で、ω ∉ Q(∛2)（実体は実数）より `[Q(∛2,ω):Q(∛2)] = 2`。よって `[L:Q] = 6`。

**練習問題 4.2** 解答: 根は ±2^(1/4), ±i·2^(1/4)。分解体 L = Q(2^(1/4), i)。`[Q(2^(1/4)):Q] = 4`（X⁴−2 は既約）。i ∉ Q(2^(1/4)) だから `[L:Q] = 8`。

**練習問題 4.3** 解答: `f(X) = X³+X+1` は f(0)=1, f(1)=1 より根なし、3次なので既約。分解体は F_8（8元体）。元の個数 8。

**練習問題 4.4** 解答: `X³−1 = (X−1)(X²+X+1)`。分解体は Q(ω)（ω は X²+X+1 の根，1の原始3乗根）。`[Q(ω):Q] = 2`。

**練習問題 4.5** 解答: F を有限体で代数閉体とする。`g(X) = 1 + ∏_{a∈F}(X−a)` を考える。g は F 上で根を持たない（各 a ∈ F で g(a) = 1）。しかし非定数多項式は根を持つ（代数閉体の定義）から矛盾。よって有限代数閉体は存在しない。

### 第5章

**練習問題 5.1** 解答: `f'(X) = 3X²+1 = X²+1`（標数2）。ユークリッド互除法: `X³+X+1 = X·(X²+1) + 1`。よって `gcd(f, f') = gcd(X²+1, 1) = 1`。したがって f は分離的。

**練習問題 5.2** 解答: `X^p − t ∈ F_p(t)[X]`。アイゼンシュタインの判定法を F_p[t] の素元 t について適用すると既約。分解体で `X^p − t = (X − t^(1/p))^p` と重根を持つから非分離。

**練習問題 5.3** 解答: F_4 = {0, 1, x, x+1}，x² = x+1。F_4* = {1, x, x+1} は位数3の巡回群で、x は生成元（x² = x+1, x³ = x·(x+1) = x²+x = 1）。よって F_4* ≅ Z/3Z。

**練習問題 5.4** 解答: `φ: a ↦ a²`。φ³ = id（a^8 = a）。φ ≠ id（X²−X の根は F_2 のみ）なので位数 3。よって Gal(F_8/F_2) ≅ Z/3Z。

**練習問題 5.5** 解答: F_q* は位数 q−1 の群だから、a ∈ F_q* で a^(q−1) = 1，よって a^q = a。a = 0 でも成立。

### 第6章

**練習問題 6.1** 解答: Q(√2,√3) は (X²−2)(X²−3) の分解体なので正規。

**練習問題 6.2** 解答: 既約多項式 X²−2 の根 √2, −√2 はどちらも Q(√2) に含まれるから正規。X³−2 は既約だが、根 ∛2 の共役 ∛2ω は Q(∛2)（実数の部分体）に含まれないから Q(∛2)/Q は正規でない。

**練習問題 6.3** 解答: 正規でない。X⁴−2 の根 2^(1/4) の共役 i·2^(1/4) が Q(2^(1/4))（実体）に含まれない。

**練習問題 6.4** 解答: 正規閉包は X³−2 の分解体 Q(∛2, ω)。

**練習問題 6.5** 解答: 正規拡大の定義そのもの。f が L に根 α を持てば、f は既約なので α の最小多項式は f。定義により f は L 上で一次式の積に分解する。

### 第7章

**練習問題 7.1** 解答: Gal(L/Q) = { id, σ, τ, στ }，σ(√2)=−√2, τ(√3)=−√3。|Gal(L/Q)| = 4 = [L:Q]。

**練習問題 7.2** 解答: 各 √2, √3, √5 の符号の反転で 2³ = 8 個。Gal ≅ (Z/2Z)³。位数 8。

**練習問題 7.3** 解答: Gal(Q(ζ_5)/Q) ≅ (Z/5Z)* ≅ Z/4Z。生成元は「2 に対応する自己同型」σ₂: ζ ↦ ζ²。位数 4。

**練習問題 7.4** 解答: Gal(F_16/F_2) ≅ Z/4Z（フロベニウス a ↦ a² が生成元，位数 4）。

**練習問題 7.5** 解答: 必要十分条件は「L/K が正規かつ分離（=ガロア拡大）」、あるいは `[L:K] = |Gal(L/K)|`。Q(∛2)/Q は正規でないからガロア拡大でない（分離的ではあるが）。

### 第8章

**練習問題 8.1** 解答: Gal ≅ (Z/2Z)³。部分群は8個（{id} を含む）：位数 1:1個，位数 2:7個，位数 4:7個，位数 8:1個。中間体も同数。2次拡大の中間体は Q(√2), Q(√3), Q(√5), Q(√6), Q(√10), Q(√15), Q(√30) の7個、4次拡大の中間体が7個（√2,√3,√5 から2つ選んで生成した体）。

**練習問題 8.2** 解答: S₃ の部分群: {id}, ⟨(12)⟩, ⟨(13)⟩, ⟨(23)⟩（位数2，各々正規でない）, A₃ = ⟨(123)⟩（位数3，正規）, S₃。よって中間体: L（=Q(∛2,ω)）; 3つの3次中間体（位数2の部分群に対応，例 Q(∛2), Q(∛2ω), Q(∛2ω²)）; Q(ω)（A₃ に対応，Q 上ガロアで次数2）; Q。正規でない中間体は Q(∛2) など。

**練習問題 8.3** 解答: 8.1節の基本定理を正確に記述すること（全単射、順序反転、次数の対応、正規部分群とガロア拡大の対応）。

**練習問題 8.4** 解答: Q(√6) は √2 と √3 を共に固定しない部分群に対応。Gal(L/Q) = {id, σ, τ, στ} で、Q(√6) = L^⟨στ⟩（στ は √2·√3 を固定）。⟨στ⟩ は可換群 Z/2Z×Z/2Z の部分群なので正規。

**練習問題 8.5** 解答: `[F:K] = 2` と基本定理より `[G:Gal(L/F)] = 2`。指数2の部分群は常に正規部分群（gH = Hg を示す）。よって Gal(L/F) ⊴ G かつ指数 2。

### 第9章

**練習問題 9.1** 解答: ∛2 が作図可能なら [Q(∛2):Q] は2の冪。しかし [Q(∛2):Q] = 3 は2の冪でない。矛盾。よって ∛2 は作図不可能（立方体倍積の不可能性）。

**練習問題 9.2** 解答: `{e} ⊴ A₃ ⊴ S₃`。A₃ = {id, (123), (132)} ≅ Z/3Z（アーベル），S₃/A₃ ≅ Z/2Z（アーベル）。よって S₃ は可解群。

**練習問題 9.3** 解答: `{e} ⊴ V₄ ⊴ A₄ ⊴ S₄` ではなく A₄ の可解性。V₄ = {id, (12)(34), (13)(24), (14)(23)} は A₄ の正規部分群（A₄ の共役類はサイクル型で決まり、V₄ は共役で閉じる）。V₄ ≅ Z/2Z×Z/2Z（アーベル），A₄/V₄ ≅ Z/3Z（アーベル）。よって A₄ は可解群。

**練習問題 9.4** 解答: S₄ は `{e} ⊴ V₄ ⊴ A₄ ⊴ S₄` で剰余がアーベル（V₄ アーベル，A₄/V₄ ≅ Z/3Z，S₄/A₄ ≅ Z/2Z）だから可解群。ガロア群が可解群 ⇔ 冪根で解けるから、一般4次方程式は冪根で解ける（フェラリの解法が存在する事実と整合）。

**練習問題 9.5** 解答: 一般5次方程式のガロア群は S₅。S₅ は可解群でない（A₅ が単純群）。冪根可解性定理により、ガロア群が可解でない方程式は冪根で解けない。よって一般5次方程式は四則演算と冪根だけでは解けない。

### 第10章

**演習 1** 解答: Gal ≅ (Z/2Z)³，部分群は 1 + 7 + 7 + 1 = 16 個（正しくは {id} を含め 16 個の部分群）。中間体も16個。3つの生成元のそれぞれについて {id}，位数2の部分群7個（各々 Z/2Z），位数4の部分群7個（各々 Z/2Z×Z/2Z），全体。

**演習 2** 解答: フロベニウス φ: a ↦ a² が生成元。位数 3（a^8=a で φ³ = id，φ ≠ id）。よって Gal(F_8/F_2) ≅ Z/3Z。

**演習 3** 解答: 既約性: f(0) = 1, f(1) = 1（0, 1 は根でない）かつ2次因子を持つか確認。f = X⁴+X+1。既約2次多項式 X²+X+1 が割り切るか: X⁴+X+1 mod (X²+X+1) を計算すると X³=1（mod 既約多項式の根を α とすると α² = α+1, α³ = 1）で、X⁴+X+1 = α·X + X + 1... 丁寧に計算すると割り切れない。よって既約。分離性: f'(X) = 4X³+1 = 1（標数2）で gcd(f, 1) = 1 だから分離。

**演習 4** 解答: Gal(Q(ζ_7)/Q) ≅ (Z/7Z)*。Z/7Z は体だから (Z/7Z)* は位数6の巡回群 ≅ Z/6Z。よってガロア群は位数6の巡回群。

**演習 5** 解答: Q(∛2)/Q。X³−2 が既約で、根 ∛2 の共役 ∛2ω が Q(∛2) に含まれないから正規でない。

**演習 6** 解答: 定理を述べ、∛2 は [Q(∛2):Q] = 3 で2の冪でないから作図不可能。

**演習 7** 解答: (1) 一般5次方程式のガロア群 = S₅（対称式の基本定理）。(2) S₅ は非可解（A₅ の単純性）。(3) 冪根可解性定理「冪根で解ける ⇔ ガロア群が可解群」。(4) 以上より一般5次方程式は冪根で解けない。

**演習 8** 解答: 有限p群は中心が非自明（類等式）で、中心を取って剰余を繰り返すとアーベル群の正規列ができる。よって可解群。位数8の群は2群だから可解。

**演習 9** 解答: X^p − a が既約なら、分解体 L は a^(1/p) と1のp乗根 ζ_p で生成される。L/K のガロア群は、生成元 a^(1/p) ↦ ζ_p·a^(1/p) を送る写像で決まり、位数が p の約数で、既約性よりちょうど位数 p の巡回群に埋め込まれる。1の冪根が K に含まれる場合は Gal ≅ Z/pZ（巡回群）。

**演習 10** 解答: 8.1節を参照。証明の要点: (1) 単調性 (2) L^(Gal(L/F)) = F（ガロア拡大の次数公式）(3) アルティンの定理 [L:L^H] = |H|。

---

## 付録B 用語集

- **アーベル群**: 演算が可換な群（可換群）。
- **アルティンの定理**: 体 L の自己同型の有限群 G について [L:L^G] = |G|。
- **一般 n 次方程式**: 基本対称式を係数に持つ文字係数の n 次方程式。そのガロア群は Sₙ。
- **イデアル**: 可換環 R の部分群で、R の任意の元を掛けても自身に留まる集合。
- **因数分解**: 本項では多項式を一次式や既約因子の積に分解すること。
- **可解群**: 正規列で各剰余群がアーベル群になる群。
- **拡大次数**: 体拡大 L/K で、K 上の線型空間としての L の次元。記号 [L:K]。
- **ガロア群**: 体拡大 L/K に対し、K を固定する L の自己同型全体のなす群。記号 Gal(L/K)。
- **ガロア拡大**: 正規かつ分離な代数拡大。[L:K] = |Gal(L/K)|。
- **ガロア理論の基本定理**: 有限次ガロア拡大 L/K の中間体と Gal(L/K) の部分群の対応が全単射。
- **共役**: 同じ最小多項式を持つ2つの元（K 上）。
- **剰余群**: 正規部分群による剰余類全体のなす群。記号 G/H。
- **剰余類**: 部分群 H による aH = { ah | h ∈ H } の形の集合。
- **準同型**: 群・環・体の構造を保つ写像。
- **準同型定理**: G₁/Ker f ≅ Im f。
- **純非分離拡大**: すべての元の最小多項式が X^(p^n) − a の形になる拡大。
- **正規拡大**: 既約多項式の1根が含まれれば全根含まれる拡大。
- **正規閉包**: 拡大 L/K を包む最小の正規拡大。
- **最小多項式**: 元 a を根に持つモニック既約多項式で次数最小のもの。記号 irr(a, K)。
- **作図可能**: 定規とコンパスで有限回の操作で作れる点（数）。
- **固定体**: Gal(L/K) の部分群 H で不変な L の元全体。記号 L^H。
- **完全体**: すべての既約多項式が分離的な体（標数0またはフロベニウス全射）。
- **有限体**: 元の個数が有限の体。F_q（q = p^n）の形。
- **標数**: 1 を n 回足して 0 になる最小の n（なければ 0）。記号 char K。
- **分解体**: 多項式 f のすべての根を添加した最小の体。
- **分離拡大**: すべての元の最小多項式が分離的な代数拡大。
- **分離多項式**: 重根を持たない多項式（gcd(f, f') = 1）。
- **フロベニウス写像**: 標数 p の体の写像 x ↦ x^p。有限体のガロア群の生成元。
- **円分体**: 1 の冪根で生成される体 Q(ζ_n)。
- **アーベル–ルフィニの定理**: 一般の5次以上の方程式は冪根だけでは解けない。

## 付録C 記号表

| 記号 | 意味 |
|---|---|
| Z, Q, R, C | 整数環、有理数体、実数体、複素数体 |
| F_p | p元体（= Z/pZ，p素数） |
| F_q | q元有限体（q = p^n） |
| char K | 体 K の標数 |
| K[X] | K 上の多項式環 |
| K(X) | K 上の有理関数体 |
| L/K | 体拡大（L は K の拡大体） |
| [L:K] | 拡大次数 |
| K(a) | K に a を添加した体 |
| irr(a, K) | a の K 上の最小多項式 |
| K̄ | K の代数閉包 |
| K^G, L^H | 群 G（H）で不変な体の元全体（固定体） |
| Gal(L/K) | ガロア群 |
| H ⊴ G | H は G の正規部分群 |
| Sₙ | n次対称群 |
| Aₙ | n次交代群 |
| ζ_n | 1の原始 n 乗根 |
| φ(n) | オイラーのφ関数 |
| (Z/nZ)* | nと互いに素な剰余類のなす乗法群 |
